{
  "name": "dynamic-flip",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Turn wide video into tall video, in the browser, with a hand-steered crop path.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test:e2e": "node tests/e2e.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "mediabunny": "^1.56.2"
  },
  "devDependencies": {
    "playwright": "^1.63.0",
    "typescript": "^5.9.3",
    "vite": "^6.4.3"
  }
}
